# Frontend (Vite) env vars
# Vite only exposes vars prefixed with VITE_

# Optional: override API base if needed
# VITE_API_BASE=/api

# Optional: dev-only token (avoid in production)
# VITE_BUFFER_ACCESS_TOKEN=
